# Aerospike (internal container port, used by compose.yaml)
AEROSPIKE_HOST=aerospike-node-1
AEROSPIKE_PORT=3000

# Aerospike (host-mapped port for local dev with compose.dev.yaml)
# AEROSPIKE_HOST=localhost
# AEROSPIKE_PORT=14790

# Backend
BACKEND_PORT=8000
CORS_ORIGINS=http://localhost:3100

# Frontend
FRONTEND_PORT=3100
