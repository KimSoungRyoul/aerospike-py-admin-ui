import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Secondary Indexes | Aerospike Cluster Manager",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
